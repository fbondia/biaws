// Keep this published copy synchronized with shared/documentTypes.js. The MCP
// package cannot import files outside its package root; the parity test prevents
// the API and MCP contracts from drifting.
export {
  DOCUMENT_TYPE_CATALOG,
  DOCUMENT_TYPES,
} from "./documentTypeCatalogData.js";
